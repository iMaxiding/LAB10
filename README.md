## Konfiguracja

Aplikacja korzysta z pliku `.env` do konfiguracji połączenia z bazą danych.
W repozytorium znajduje się plik `.env.example` z wymaganymi zmiennymi.

Wymagana zmienna:
- `DB_URL=jdbc:sqlite:database.db`